import { hello } from './hello.js';
const numberOfChild = 45;
hello.charAt(0);
if (numberOfChild > 3) {
    console.log('hola');
    for (let i = 0; i < 100; i++) {
        console.log('adios');
    }
}
export const demoTest = 'Hello';
